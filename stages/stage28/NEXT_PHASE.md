# Next phase — Stage 29

Add RGBA8 image/memory resources, a minimal shader module and an offscreen render target, then issue a deterministic draw/readback when a Vulkan ICD is available. Keep presentation out until the render path is proven.

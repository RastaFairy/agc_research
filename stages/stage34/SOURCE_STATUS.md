# Source status

PS5 Payload SDK: real dynamic-linking SDK for exploited PS5 payloads.
The repository documents generating SCE stubs from decrypted SPRX files and resolving them dynamically.

KytyPlus / SharpEmu: used only as semantic references for AGC/guest GPU behavior.
They are not treated as sources for Sony C ABI declarations.

Stage 25 IR: retained as semantic reference only.

Stage 26-33 Vulkan host work: retained as a host-side oracle/test bench, not as the PS5 final backend.
